import { Header } from "components";
import React from "react";
import { SafeAreaView } from "react-native";

const ReadNovelScreen = () => {
  return (
    <SafeAreaView>
      <Header title="Read Novel Screen" titleOn="left" withBackIcon />
    </SafeAreaView>
  );
};

export default ReadNovelScreen;
